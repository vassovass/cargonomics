# Deferred Items — Cargonomics Website

**Purpose.** Durable record of site issues and observations noticed during work but not fixed in the same change. Distinct from `03-project-management/open-questions.md`, which tracks items blocked on PEOPLE. This file tracks items blocked on TIME or PRIORITY — code and UX observations that can be addressed unilaterally when scheduled.

**Anti-duplication rule.** See `.claude/rules/website-conventions.md` Tracker Discipline section. An item never carries `open` status in both trackers simultaneously.

**Status flow.** `open` → `in-progress` (linked to active work) → `scheduled` (linked to a PRD number) → `done` (linked to commit SHA or PRD closing date).

**When to use which tracker:**
- Does fixing it require waiting on someone? → `03-project-management/open-questions.md`
- Can we just code it when priority allows? → this file.

**Chat-ask logging rule (new, 2026-04-16).** See `.claude/rules/deploy-protocol.md` "Chat-ask logging" section. Any user ask stated in chat is logged here in the same turn, BEFORE the work begins, so no ask drops silently.

---

## Active items (open + in-progress + scheduled)

| # | Date added | Observed during | Description | Impact | Proposed fix | Effort | Owner | Status |
|---|------------|-----------------|-------------|--------|--------------|--------|-------|--------|
| D1 | 2026-04-14 | Process A seed (Apr 14 planning) | `course.html` testimonials section is an empty `.course-placeholder` | Low. Reduces course page credibility signal below what PRD 5 envisaged. | Hide the section until real testimonials exist (Commit 1 of Apr 16 plan). Long-term: gather 2-3 testimonials from prior conference attendees or pilot cohort. | Small (hide) / Medium (content gather) | Vasso | done (96744fb) |
| D2 | 2026-04-14 | Process A seed | `course.html` FAQ section empty placeholder | Low. Applicants have unanswered practical questions (cost, time commitment, post-graduation commitment). | Hide for now. Long-term: draft 6-8 FAQ items from recurring WhatsApp questions. Render as accordion inside existing `.course-placeholder` block. | Small (hide) / Small (content + accordion CSS) | Vasso | done (96744fb) |
| D3 | 2026-04-14 | Process A seed | `course.html` instructors section shows only placeholder card beyond Elias | Medium. Single-leader framing weakens the "learn from veterans" promise. | Hide for now. Long-term: recruit 2-3 named trainers with photos and bios. | Small (hide) / Medium (content + pending Marilyn) | Vasso | done (96744fb) |
| D4 | 2026-04-14 | Process A seed | Homepage second trainer card is a placeholder ("Senior Industry Advisor") | Medium. Visible placeholder erodes trust. | Remove the card entirely for now. Reinstate when real partner named. | Small | Vasso | done (96744fb) |
| D5 | 2026-04-14 | Process A seed | Homepage "Why Us" section has structure but placeholder copy | Medium. MVP ships with visible placeholders where Marilyn's bullets belong. | Replace placeholder copy with Marilyn's bullets when that input arrives. Cross-reference: open-questions item for Why Us bullets. | Small | Vasso / Marilyn | open (blocked on Marilyn content) |
| D6 | 2026-04-14 | Process A seed | Application form lacks post-submit success-state styling (`.form--submitted` class declared but unstyled) | Low. Submission still works via GAS backend but visual feedback is minimal. | Add a success card with check icon, confirmation copy, and what-to-expect-next. Style the `.form--submitted` class. | Small | Vasso | open |
| D7 | 2026-04-11 | PRD 3 | `.hero__media` class needs CSS rule or verify `.hero__video` rules apply | Low | Verify, add rule if needed | Small | Vasso | open |
| D8 | 2026-04-11 | PRD 3 | `.footer__social-link` class needs CSS for SVG icon sizing and spacing | Medium | Add hover state, spacing, and alignment CSS rules | Small | Vasso | done (superseded by D59) |
| D9 | 2026-04-11 | PRD 4 | CLAUDE.md says "former CEO of ZIM Shipping Vietnam" but LinkedIn says "Managing Director". Used "Managing Director" on About page. | Low. Fact-checking only, no code change needed. | Correct CLAUDE.md | Trivial | Vasso | open |
| D10 | 2026-04-11 | PRD 4 | About page company-profile section has no hero image | Medium | Consider adding a hero image (office exterior, team photo, or 3D shipping visual) once Marilyn provides DepositPhotos. | Medium | Vasso / Marilyn | done (96744fb) |
| D11 | 2026-04-11 | PRD 4 | Partner trainer placeholder uses generic "IE" initials. | Low. Tracked in marilyn-deliverables-checklist. | Update when partner details arrive. | Small | Vasso / Marilyn | superseded (D4 done in 96744fb) |
| D12 | 2026-04-11 | PRD 5 | Day 5 port/dock facility visit has no named facility. | Low. Update course.html schedule + JSON-LD when Marilyn confirms venue. | Update copy when named | Small | Vasso / Marilyn | open (blocked on Marilyn) |
| D13 | 2026-04-11 | PRD 5 | Course PDF (deliverable 2b) still not received. Course page copy drafted from homepage marquee and transcript, marked PLACEHOLDER. | Medium. Blocks final copy approval. | Replace when PDF arrives | Medium | Vasso / Marilyn | open (blocked on Marilyn) |
| D14 | 2026-04-11 | PRD 6 | Airtable mapping gap: PRD 6 visible fields omit `university` and `graduation_year` which are in airtable-conventions.md 4.1. | Low. Does not block MVP. | Decide to add to form or treat as CRM-only | Small | Vasso | open |
| D15 | 2026-04-11 | PRD 6 | WhatsApp/Zalo link destinations: contact-info section and floating buttons use `href="#"` placeholder. | Medium. Blocks real messaging but not MVP launch. | Update once Marilyn provides business numbers | Small | Vasso / Marilyn | open (blocked on Marilyn) |
| D16 | 2026-04-11 | PRD 6 | PDPD consent text is placeholder. | Medium. Not blocking MVP. | Legal review against Decree 13/2023/ND-CP before production launch | Medium | Vasso | open |

---

## Apr 16 session intake (NEW — from chat-asks backlog audit)

Items below were identified by auditing past session logs on 2026-04-16 after Vasso flagged that asks were dropping. Entering them here with full provenance.

| # | Date added | Source session | Description | Impact | Proposed fix | Effort | Owner | Status |
|---|------------|----------------|-------------|--------|--------------|--------|-------|--------|
| D17 | 2026-04-16 | Apr 14 session | **Trusted Partners section on homepage.** Vasso: "trusted partners section that needs to be added in... with logos... but for now, it might just be a list of who the partners are... social validation." Text-based now, logo-ready for later. Seed from Elias' verified career history (APL, CMA CGM, ZIM) + VN market majors. | High. Social validation is a named ask. Marilyn has not seen this ask addressed. | Build `.trusted-partners` section with text tiles; `data-partner-name` + `data-partner-logo-src` ready for future logo swap. | Medium | Vasso | done (dcb56db) |
| D18 | 2026-04-16 | Apr 14 session | **Trusted Partners on course page.** Adapted version of D17 — same section or reworked for course context. | High. Same ask as D17. | Same module, placed between `.why-apply` and `.curriculum-grid`. | Medium | Vasso | done (dcb56db) |
| D19 | 2026-04-14 | Apr 14 session | **Apply page split.** Marilyn's instruction per Vasso: separate `apply.html` for prospective applicants; `contact.html` stays a general inquiry form. | High. Structural. Marilyn-requested. | Create `apply.html`, move application form; strip from contact.html; update nav + CTAs + sitemap + redirects. | Medium | Vasso | done (ddb5d1f) |
| D20 | 2026-04-14 | Apr 14 session | **Extra form questions from Marilyn** pending. | Medium. Form incomplete until received. | Add fields when Marilyn sends them. | Small | Marilyn | open (blocked on Marilyn) |
| D21 | 2026-04-16 | Apr 16 session | **About hero image stretched landscape.** Vasso: "this image looks weird being so landscape". Currently `port-1.jpg` with no aspect constraint. | High. Visible immediately on About page load. | Swap to `hero-home.jpg` with `<picture>` + srcset. Add `object-fit: cover` with max-height. | Small | Vasso | done (96744fb) |
| D22 | 2026-04-16 | Apr 16 session | **Three Pillars on About is text-heavy.** Vasso: "very text heavy... make it look better". | High. Visible immediately on About page scroll. | Add image headers reusing `coach-pillar`, `connect-conference`, `consult-conference` images. Promote numbering. Trim closing meta-sentence from each body. | Medium | Vasso | done (96744fb) |
| D23 | 2026-04-14 | Apr 14 session | **Image/visual to break up text-only homepage spot.** Screenshot reference, exact spot unclear. | Medium. Improves scannability. | Vasso to point at specific spot in a future chat. | Small once located | Vasso | **open (blocked on Vasso clarification)** |
| D24 | 2026-04-14 | Apr 14 session | **Mobile-responsive audit, WordPress/Elementor-forward.** Full layout assessment and fix pass. | Medium-High. Mobile is primary for VN students. | Dedicated PRD 22 — mobile audit spanning all pages, every breakpoint, with token adjustments and BEM refactors where needed. Run via Claude Preview at 360/375/414/768/1024. | Large (3+ hours) | Vasso | done (735ab7b; PRD 22 shipped v7 fixes — tokens, inline-to-class, touch targets) |
| D25 | 2026-04-14 | Apr 14 session | **Color picker + reset in footer** for brand-color experimentation. Modular, removable before WordPress migration. | Medium. Internal dev tool for design iteration with Marilyn. | Dedicated PRD 23 — vanilla JS widget in a corner of the footer, writes to CSS custom properties, persists in localStorage, reset button clears. | Medium | Vasso | done (21eba89; PRD 23 devtool color picker live via ?devtool=1) |
| D26 | 2026-04-14 | Apr 14 session | **Module / pattern library page** (`styleguide.html`). Every section rendered in isolation, naming conventions documented, WP-theme-kitchen-sink style. | Medium. Serves design iteration + WP migration handoff. | Dedicated PRD 24 — `styleguide.html` rendering each component (hero, pillar, trainer card, program-featured, curriculum-marquee, etc.) with its class names, tokens used, and Elementor target. | Large | Vasso | done ([PRD 24 commit]; styleguide.html 10 components + metadata, noindex/nofollow) |
| D27 | 2026-04-16 | Apr 16 session | **Site review before Marilyn** — confirm v4/v5/v6 state is presentable. Sent with CTA + one-pager. | High. Marilyn about to review. | Run full placeholder audit + visual scroll of all pages post-Apr 16 commits. | Small | Vasso | done (Apr 16 session audit complete; all visible placeholders closed) |
| D28 | 2026-04-11 | Apr 11 session | About page right-side blank space + "whole long footer" weird | Medium | Verify after Commit 1 fixes land; may already be resolved by hero image swap + pillars redesign | Small | Vasso | done (96744fb; Apr 11 right-side blank space fixed by 2-col grid + hero image) |
| D31 | 2026-04-17 | Apr 17 session | **Styleguide needs a "Used on pages" log per component** + a process/rule so it stays accurate. | High. Makes styleguide self-documenting for WordPress migration + Marilyn handoff. | Added `<dt>Used on:</dt>` row to all 13 sg-sections in `styleguide.html`. Populated from live page state. Added "Styleguide Usage Log" rule to `.claude/rules/website-conventions.md`. Cross-referenced in styleguide `.sg-notice` intro. | Medium | Vasso | done (162482c) |
| D32 | 2026-04-17 | Apr 17 session | **"go live" must push to BOTH dev and live branches** (in parallel), never bypass dev. | High. Process rule that affects every future deploy. Prevents dev/live drift. | 1) `staging` branch created on both repos from main. 2) `.claude/rules/deploy-protocol.md` updated with BOTH-branches semantics + Cloudflare-is-live correction. 3) Feedback memory updated cross-session. Core rule is LIVE. Maintenance-parking amendment (when `main` is deliberately parked on maintenance, default target becomes staging-only) is still PENDING in the rule doc. | Small | Vasso | in-progress (core rule live in parent 593417a + 481371b; maintenance-parking amendment pending) |
| D30 | 2026-04-17 | Apr 17 session | **Styleguide technical details should collapse by default.** | High. Makes styleguide presentable to Marilyn/Elias without stripping dev-useful data. | Wrapped each section's top `.sg-desc` + `.sg-meta` in native `<details class="sg-details"><summary>Technical details</summary>...</details>`. Default collapsed. Gold [+]/[-] toggle. Inline mini-labels stay visible. CSS added to `fixes.css`. Applied across all 13 sg-sections. | Medium | Vasso | done (162482c) |
| D33 | 2026-04-17 | Apr 17 session | **Show git commit SHA next to `v7` in footer.** Vasso: "have the commit number shown correctly for the live site... and the commit number for the dev site. And they may be different because we are editing the site as we go along." | High. Makes "which deploy am I looking at" answerable at a glance. Live vs dev divergence becomes visible to Marilyn. | Use Cloudflare Pages Functions `_middleware.js` + `HTMLRewriter` to append `env.CF_PAGES_COMMIT_SHA` (first 7 chars) to `<span class="footer__version">` at request time. Cloudflare auto-sets that env var per deployment, so `staging` shows staging SHA and `main` shows main SHA with no extra wiring. Wrap SHA in inner `<span class="footer__commit">` for styling. Local `python -m http.server` and GH Pages mirror keep bare `v7` (fine — secondary). | Small | Vasso | done (d551091 JS v1; ff03754 middleware v2) |
| D34 | 2026-04-17 | Apr 17 session | **Revert live trainer section to v2.** Vasso: "the old version of the Training, because this looks ridiculous. That portrait look and whatever else looks ridiculous, both on mobile and on desktop. Let's just have the old version that was there because you really messed it up. Trainer card v2 is wanted." v3 editorial deployed speculatively, rejected after visual review. | High. User visual rejection overrides speculative approval. | Swap `index.html` `#trainers` + `course.html` `#course-instructors` from `.trainers-v3` / `.trainer-card-v3--editorial` back to `.trainers-v2` / `.trainer-card-v2`. Styleguide v2 section un-deprecated. v3 A/B/C sections retained in styleguide marked "explored, not live". First attempt (960dbb2) lost when `920c704` "restored normal index.html + _redirects for staging" inadvertently resurrected v3 markup from the pre-revert tree state; re-applied in `662fbc9`. | Medium | Vasso | done (662fbc9) |
| D35 | 2026-04-17 | Apr 17 session | **Placeholder trainer card background should be white**, not gold-tinted. Vasso: "why is the second... The Senior Industry Advisor one, why is that-- the background for that a different color? It should be white as well." Applies to `.trainer-card-v2--placeholder`, `.leader-card--placeholder`, `.trainer-card-v3--placeholder`. | High. Visible mismatch vs regular card on same row. | CSS `background` in `fixes.css` changed from `rgba(var(--color-secondary-rgb), 0.04-0.05)` to `var(--color-white)` for all three placeholder modifiers. Dashed gold border + gold "Coming soon" badge retained as the "reserved slot" signal. Also lost in the 920c704 rebase chain, re-applied in 6c97f48. | Small | Vasso | done (6c97f48) |
| D29 | 2026-04-17 | Apr 17 session | **"Your Instructors" feels lazy.** Vasso: want three forward-thinking variants (A editorial + chips, B network motif, C split-panel reveal) all rendered in styleguide; variant A lands live on `index.html` and `course.html`. | High. Current v2 reads as generic bio card; weakens "industry veterans" promise. | Built `.trainer-card-v3` with 3 variant modifiers (`--editorial`, `--network`, `--split`). v3-A was deployed live speculatively, then rejected by Vasso on visual review and reverted (see D34). v3 variants retained in styleguide for reference; v2 remains live default. Portrait-gen via Nano Banana blocked (MCP sandbox cannot read local files). Marilyn deliverable 7a added for proper portrait. | Medium-Large | Vasso | done (162482c built; reverted per D34/D35; v3 CSS + styleguide retained) |
| D36 | 2026-04-21 | Apr 21 session | **Activate real partner logos in `trusted-partners` marquee.** Vasso dropped 10 real partner logos (CMIT, Gemadept, Hateco, Headway Logistics, KLN, LX Pantos, OOCL, UPS, Vantage, VinaLogistics) into Downloads. | High. Replaces the 11 fake typographic wordmarks (CMA CGM, ZIM, Maersk, MSC, Hapag-Lloyd, ONE, Saigon Newport, Gemadept, Tan Cang, DB Schenker, DHL) that have shipped since 2026-04-16. Social-validation band now carries real brand marks. | 1) Archive originals in `06-client-comms/assets-from-marilyn/2026-04-20-partner-logos/` with README. 2) Stage normalized `partner-<slug>.<ext>` copies in new `img/partners/` subfolder. 3) Swap `<span class="trusted-partners__logo">` placeholders for `<img class="trusted-partners__logo-img">` on `index.html`, `course.html`, `styleguide.html` main + aria-hidden duplicate tracks. 4) Drop `data-logo-style` attribute site-wide (was only used for fake-wordmark typography). 5) Update styleguide technical-details paragraph to past tense. 6) Populate `data-partner-logo-src` (was empty strings) to match `src` for future Elementor ACF mapping. | Medium | Vasso | done (6708f9c) |
| D37 | 2026-04-21 | Apr 21 session | **Marilyn popup copy markup** (annotated screenshot, WhatsApp/email). Updates desktop modal + mobile sticky bar on `index.html` with: program name "Shipping & Logistics 101 Career Development Program", "1 June 2026" start, "5 days" duration, "selected few / only 30 slots for the best", "more than 50 years combined experience", and named partners "Gemadept, Vina Logistics, Saigon Port" (verbatim per Vasso's call, even though Saigon Port is not in the 10-logo marquee). | High. Pop-up is the highest-converting CTA on the site. Previous copy contained a stale May 2026 date, wrong 4-day duration, and generic "hiring companies". | Edit index.html modal + sticky bar. Fix program card "Applications Open: May 2026" → "Now". Update memory `project_agreed_terms.md` May→June. | Small | Vasso | done (03b03fb) |
| D38 | 2026-04-21 | Apr 21 session | **Marilyn hero + photo-swap + program-card markup.** Hero H1 becomes "Fast track your career from classroom to job in shipping and logistics"; body paragraph updated to "five days, 4 classroom + 1 port" to match new duration (silently reconciled with Marilyn's Program page markup). Swap Coach/Connect/Consult photos: Connect now uses the current `consult-conference.jpg`, Consult now uses `connect-conference.jpg` (filenames unchanged, attributes swapped). Remove "for each cohort" from journey step 3. | High. Hero is primary first impression; photo swap is Marilyn's visual direction. | Edit index.html. | Small | Vasso | done (03b03fb) |
| D39 | 2026-04-21 | Apr 21 session | **Marilyn PROGRAM PAGE overhaul.** Applies Marilyn's Program-Page markup to `course.html` (and propagates to `apply.html`, `about.html`, `contact.html`, `styleguide.html` where same strings appear). Changes: Student Immersion Training Program → Shipping and Logistics 101 Career Development Program (all meta/JSON-LD/copy), 4 days → 5 days (4 classroom + 1 port, Day 5 is port), Investment → Program Fee "Sponsored and free", remove any "corporate partners fund training" claim (just say "fully funded"), 25+ → 50+ years on aggregate-team copy (individual trainer 25+ bios stay — that's Elias's solo figure), remove "per cohort" (3 instances), rewrite Who Should Apply to "Academic Excellence: Final year students, fresh graduates, 1st jobbers, and candidates interested in this industry", remove "WYDLS Bootcamp" footer link across all 5 live pages. | High. Overhauls the primary conversion page. | Sequential edit pass index → course → apply → about → contact → styleguide. | Large | Vasso | done (03b03fb) |
| D40 | 2026-04-21 | Apr 21 session | **Version bump v7 → v8 bundled with Marilyn 2026-04-21 overhaul.** Vasso explicitly requested "change to version 8 with this" during the overhaul session. Per rule, version bumps are user-gated; Vasso's phrasing is unambiguous. | Low. Footer-string-only change on 5 live pages (index, about, course, contact, apply). | Update `<span class="footer__version">v7</span>` → `v8` on all 5 live pages in the same commit as the content overhaul. Commit title uses `[v8]` suffix. | Trivial | Vasso | done (03b03fb) |
| D42 | 2026-04-24 | Apr 24 session | **Module 8 missing from homepage curriculum marquee.** Vasso flagged after the 2026-04-24 overhaul (commit a9397f0) that the index.html `.curriculum-v2__track` still has 7 cards x 2 sets = 14, not 8 x 2 = 16. Boot Camp & Beyond only on course.html grid. Add the 8th card to both sets of the marquee. | Medium. Marilyn's email listed 8 modules; homepage showing only 7 is a mismatch with course.html and with her explicit spec. | Two identical .curriculum-v2__card blocks appended at the end of Set 1 and Set 2, graduation-cap SVG icon, title "Boot Camp & Beyond", back description matching course.html Module 08. No CSS change (marquee keyframes are percentage-based on total track width, 50% midpoint still seamless with 8 per set). | Small | Vasso | done (688563a) |
| D41 | 2026-04-24 | Apr 24 session | **Marilyn 2026-04-24 MVP-launch email (pre-launch content overhaul).** Apply Marilyn's email markup before today's launch: home (Coach "shipping and logistics" prefix, careers-begin heading → "Where Shipping Careers are Fast Tracked", program card: free→sponsored, drop "101" from program name, "Limited to 30-40 students"); apply form (new "Which type of company are you interested in" field with 6 options; function list: Operation→Operations, remove Analytics & Planning, Warehouse→Logistics, remove Procurement & Sourcing, Supply Chain→Port, remove Other; "free"→"sponsored program (no fees)" across all); modules renamed with Marilyn's names across home + course curriculum grids + Module 8 added on course.html; remove corporate logo marquee (hide until conference photos/flyers/videos arrive); about: add We Coach/Connect/Consult paragraph, replace "3 Pillars of Impact" heading, update Elias role to Co-Founder/Coach (no Anne Hill), Anthony role → Co-Founder/Coach; email hr@cargonomics.com.vn swap site-wide; Elias photo replacement (Marilyn's Apr 24 version → img/elias.jpg); footer already v9 from parallel session. | Critical. MVP launches today. Marilyn explicit: "once this is done, we will launch it asap today". | Single-pass sweep across index, course, apply, about, contact, styleguide. Committed + pushed to staging only. Blockers (Kanni phone, Bala Elias bio, Anthony bio+photo, conference photos, china-students photo ID, Coach/Connect photo swaps) logged as OQ #31-#36. | Large | Vasso | done (a9397f0) |
| D41 | 2026-04-21 | Apr 21 session | **Apply form: 7 new fields + Maybe-triggered follow-up, mirroring Vasso's edits to Marilyn's Google form.** Adds English Proficiency (1 to 10 self-rated scale + qualification dropdown, both required), Course date pre-selected dropdown (1 June 2026), Schedule callout above availability questions (5 consecutive days, 1 to 5 June 2026, daily times TBC), Maybe option + follow-up textarea ("What would help you commit?") on the 5-day commitment question, How did you hear about us checkboxes (LinkedIn, Facebook, Referral, University Events/Newsletter, Other) + free-text specify, Preferred working location dropdown (HCMC, Cai Mep/Vung Tau, Binh Duong/Dong Nai, Hai Phong, Hanoi, Da Nang, Other in Vietnam, Abroad) + Other specify, Prior internships Yes/No + free-text where, Work eligibility in Vietnam Yes/No (citizen/PR, work permit, would need sponsorship). | High. Brings the public-facing site form structure in line with the Google form Marilyn's team is now using, gives Airtable a structured field set to filter on, and removes free-text columns that are hard to triage. | Edit `apply.html` (new fieldsets, schedule callout, JS toggle for Maybe follow-up, additional inline CSS for callout + checkbox group). Edit `js/form-submit.js` (multi-value field support so the "How did you hear" checkbox group lands as a comma-separated cell). `docs/google-apps-script.js` already auto-evolves the sheet schema (7/7 tests passing) so the new fields land as new columns automatically. After paste-and-redeploy of the GAS, no further backend work needed for future field additions. | Medium | Vasso | done (35e9c60, staging only; live untouched) |
| D42 | 2026-04-21 | Apr 21 session | **Why Apply section on apply.html should be collapsible, default closed.** Vasso: "the why apply section should be togglable to open or close - default is closed. also look at spacing visually please". | Medium. The Why Apply block sits between the hero and the application form. Always-open it pushes the form below the fold on smaller viewports. Collapsing by default gives the form more visual prominence. | Wrap the Why Apply heading + ul in a native `<details>` / `<summary>` so it works without JS. Style summary to match the existing h2 (Orbitron, navy), add +/− toggle indicator on the right, hover state, smooth padding. Hide default disclosure marker. Spacing review pass on the rest of the new form fieldsets in the same edit. | Small | Vasso | in-progress |
| D42 | 2026-04-21 | Apr 21 session | **Add Vietsea as 11th partner in marquee.** Vasso dropped `Vietsea logo.png` into Downloads post-D36. | Medium. Adds social-validation breadth from 10 to 11 partner logos on the marquee. | 1) Archive original in `06-client-comms/assets-from-marilyn/2026-04-20-partner-logos/` (same folder as D36 batch, preserves verbatim filename "Vietsea logo.png"). 2) Stage normalized `partner-vietsea.png` in `img/partners/`. 3) Insert Vietsea between Vantage and VinaLogistics (alphabetical) in `index.html`, `course.html`, `styleguide.html` main + aria-hidden duplicate tracks. 4) Update README inventory (10 -> 11, add received-date column). 5) Update `.claude/rules/website-conventions.md` partner count. | Small | Vasso | done (commit SHA pending) |
| D43 | 2026-04-21 | Apr 21 session | **Crop the homepage hero globe for artistic effect.** Marilyn (WhatsApp): "Please crop a little bit of the globe to make it look slightly more artistic and creative." On clarification ("more globe / less ship, or less globe / more ship?") she repeated "Please crop a bit of the globe". Interpretation: standard photo-editing sense, trim away part of the sphere so it bleeds off the frame edge rather than showing a full centered sphere; makes the composition feel designed rather than stock-photo. Applies to both mobile and desktop. | High. Hero is the primary first impression on index.html. Currently `.hero__video-wrapper` has zero CSS (natural 16:9 render in the 45% grid column), so there's no crop control at all. | CSS-only first pass in `style.css`: give `.hero__video-wrapper` an `aspect-ratio`, `overflow: hidden`, and an `<img>` child rule with `object-fit: cover` + `object-position`. Desktop (≥768px): taller ratio (4/5) + `object-position: 72% 50%` to bleed globe off the left edge. Mobile (<768px): wider ratio (16/10) + `object-position: 65% 50%` to keep globe + ship both readable on small screens. Single source asset preserved, tweakable in one line if Marilyn wants "more ship" / "less globe". Staging-only push, Marilyn approves before live. Also quietly closes D7 (`.hero__video-wrapper` had no CSS rule). Verified numerically: desktop ~21% globe-off-left, mobile (4:3) ~16% globe-off-left. Screenshot verification blocked (preview_screenshot timed out repeatedly this session); visual check deferred to Marilyn on the staging URL. | Small | Vasso | in-progress (pushed to staging, awaiting Marilyn visual approval) |
| D45 | 2026-04-21 | Apr 21 session | **Google Drive folder for applicant CV uploads.** Folder created 2026-04-21 (URL https://drive.google.com/drive/folders/1cm_Y5yUuzTUatlt4ydVY4xuUFWLDt7rp), folder ID pasted into `docs/google-apps-script.js`. Still needs Apps Script redeploy to go live. | High. Until redeployed, CV/Resume and other-attachment uploads will still land as `[PENDING DRIVE SETUP]` markers because the live Web App URL points at the old (pre-upload) code. | Remaining steps: 1) Open the Apps Script editor bound to the submissions Sheet. 2) Paste the current contents of `docs/google-apps-script.js` into `Code.gs`. 3) Deploy > New deployment (NOT Manage > Edit). 4) If the Web App URL changes, update `js/form-submit.js` `APPS_SCRIPT_URL` and commit. | Small | Vasso | in-progress (folder ID set; Apps Script redeploy pending) |
| D44 | 2026-04-21 | Apr 21 session | **Vasso v8 site edits (pre-client), bundled with v8→v9 bump.** Google Doc + PDF dated 2026-04-21: footer socials not faded + brand-coloured on all pages; partner logos keep full colour (removed grayscale overlay); apply-page headline wrap; field labels share legend's Orbitron font; English fluency reduced to Beginner/Intermediate/Advanced/Fluent; English qualification became free text; band/score removed; University/Major/GPA added to Personal Information; attendance questions merged into one Yes/No; NEW logistics-interest checkbox group (with "I am not sure yet" option); NEW required CV/Resume + optional other-attachment upload wired end-to-end to Google Drive via Apps Script. v8→v9 on all 5 live pages. CV upload backend gated on D45 (Drive folder ID). | High. Visible chrome + structural form changes before client review. | Sequential edit pass across CSS (partner + socials + label font), apply.html (headline + form overhaul), form-submit.js + google-apps-script.js (file upload pipeline), footers on 4 other pages. Dispatched as 4 parallel agents. | Medium | Vasso | done (0c7e484 in sub-repo) |
| D47 | 2026-04-21 | Apr 21 session | **(was D46 in intake; renumbered) WYDLS modal gold banner looks weird.** Vasso: "please see this - looks weird" + screenshot of the desktop pop-up gold header (`.modal__free-banner`). Two visible issues: (1) "SHIPPING & LOGISTICS 101" wraps onto two lines ("SHIPPING & LOGISTICS" / "101") because `font-size: 1.6rem` + `letter-spacing: 3px` overflows the 440px modal width minus padding; (2) the × close button in the top-right is barely visible — white at 0.7 opacity on the gold banner background, reads as a stray tick mark. | High. Modal is a conversion surface. Broken title and invisible close button undermine trust the first time a visitor sees it. | In `css/style.css`: reduce `.modal__free-banner-big` letter-spacing from 3px → 2px and font-size from 1.6rem → 1.35rem so "SHIPPING & LOGISTICS 101" fits on one line. Make `.modal__close` solid white (remove 0.7 opacity) and bump font-size for visibility against the gold banner background. Added `white-space: nowrap` on the big title as belt-and-braces; widened close-button padding for an easier hit target. Verified via getComputedStyle at 1280px viewport: title renders as 1 line (was 2), banner stays at 440px, close reads as solid white 28px. | Small | Vasso | done (ce81d3d, staging only) |
| D49 | 2026-04-22 | Apr 22 session | **WYDLS modal banner: bump title to 2.25rem.** Vasso side-by-side screenshot: 2.25rem variant reads much stronger than the 1.5rem version from D48. "Logistics 101" at larger size reinforces the course name as a brand lockup. | Low-Medium. Headline prominence, first visual hit when the modal opens. | In `css/style.css` `.modal__free-banner-big` font-size 1.5rem → 2.25rem, line-height 1.15 → 1.1 (tighter stack at larger size). | Trivial | Vasso | done (6665cda, staging only) | [D49|
| D48 | 2026-04-22 | Apr 22 session | **WYDLS modal banner: intentional 2-line lockup.** After the D47 single-line fix landed on staging, Vasso reviewed and said "the title should be on 2 lines with 'shipping &' then 'logistics 101'" — treating the stack as a deliberate lockup rather than a wrap bug. "Logistics 101" stays together as the course name. | Medium. D47 forced one line via `white-space: nowrap` at reduced 1.35rem size, visually cramped. 2-line lockup gives the title more impact and reads as designed, not squeezed. | Split the title string into two `<span class="modal__free-banner-line">` blocks ("Shipping &" / "Logistics 101"), drop `white-space: nowrap`, add `.modal__free-banner-line { display: block }`, bump font-size back to 1.5rem with `line-height: 1.15`. Verified at 1280px viewport: two stacked lines, 24px font, banner height 55px. | Small | Vasso | done (aa4b0a9, staging only) |
| D50 | 2026-04-23 | Apr 23 session | **Marilyn copy edits on apply.html hero.** WhatsApp 17:08: (1) capacity "Only 30 places" → "Only 30 - 40 places"; (2) date pill "Monday 1 – Friday 5 June 2026" → "June 1st – June 8th 2026". Scope confirmed by Vasso as apply page only; screenshot shows `.contact-hero` section on `apply.html`. | High. Capacity is part of Marilyn's commercial positioning; date string visible above the application form. | Edit `apply.html` lines 650 + 653. Update `<time datetime>` attributes to match new end date (2026-06-08). FLAG downstream inconsistencies: course.html line 369 still says "Only 30 places" in same sentence; course.html schema lines 73-74 carry `startDate 2026-06-01 / endDate 2026-06-05`; multiple "5 Days Intensive" / "5-day program" strings across course, about, apply. Do NOT touch those without Vasso's call — the duration conflict needs human judgment (is bootcamp now 8 days or is Marilyn rephrasing an unchanged 5-day course?). | Small (apply only) / Medium (site-wide propagation) | Vasso | done (6bb9d60, staging only; downstream "5 Days" / schema dates / course.html "30 places" still open pending Vasso call on whether bootcamp is extending or just re-phrasing) |
| D54 | 2026-04-26 | Apr 26 session | **Regenerate Elias headshot at 2k with backdrop matching Anthony's studio look.** Vasso (chat) + screenshot of trainers grid: "can we upscale elias image make same background as anthony". Current `img/elias.jpg` (104 KB) reads as a cut-out on flat dark charcoal slab; Anthony's `img/anthony-tay.jpg` has soft light-grey studio gradient with photographic falloff. Side-by-side asymmetry visible on index/about/course trainers grids. Approval-gated: generate via nanobanana, show inline, halt for Vasso's explicit OK before swapping the file. | Medium. Cosmetic but visible on every page that lists trainers; pair currently looks mismatched. | Run `mcp__e53356a4-...__generate_image` with `input_image_path_1` = current `img/elias.jpg`, `aspect_ratio: "1:1"`, `resolution: "2k"`, identity-locked prompt describing Anthony-style backdrop. PostToolUse hook auto-saves to `D:\OneDrive\pics backup\MCP Pics\`. After Vasso approves: back up original to `img/_archive/elias-original-2026-04-26.jpg`, swap in new file as `img/elias.jpg` (re-encode to JPEG ~85 quality, target <200 KB), preview verify on index/about/course, commit `chore(img): regenerate Elias headshot at 2k with backdrop matching Anthony [v9]`, push dev only unless "go live" said. | Small | Vasso | done (8442921, staging only) |
| D55 | 2026-04-26 | Apr 26 session | **Revert about.html leadership cards from timeline back to paragraph (production format), keep timeline as styleguide-only variant.** Vasso (chat) + production screenshot of paragraph cards + Bala's PDF screenshot showing authoritative Elias bio. Asks: (1) revert both Elias and Anthony cards from `--timeline` modifier back to single `.leader-card__bio` paragraph for visual symmetry; (2) Elias bio = paragraph version of Bala's PDF content (Founder + 25y container shipping + APL/CMA CGM/ZIM history + VICT/Lotus Link board reps + SingCham chair + NUS Master's + HCM); (3) Anthony bio = production text exactly; (4) keep corrected role lines from D41/D52 ("Founder, Cargonomics" / "Senior Trainer, Cargonomics"), do NOT revert to "Co-Founder / Coach" mistake; (5) add timeline variant to styleguide.html as "explored, not live" reference per "have the timeline view as an option on styleguide though actually". | High. Reverts D52 + D53 timeline overhauls on the live About page; preserves the work as a styleguide-documented variant. | Edit `about.html` lines 217-285: drop `--timeline` modifier, replace `__lede` + `__timeline` (5 rows) + `__meta` triple with single `__bio` paragraph on both cards. Add new `<section id="sg-leader-card-timeline">` to `styleguide.html` documenting the timeline variant with sg-meta `Status: Explored, not live` + `Used on: No live pages (was live on about.html briefly via commits 1ebfca7 and 61e3105 then reverted same session)`. Update fixes.css comment block at line 446-451 to reflect new state. Keep `.leader-card__lede`, `.leader-card__timeline*`, `.leader-card__meta` CSS rules in fixes.css (still used by styleguide). | Medium | Vasso | done (0401e7f, staging only; supersedes D52 + D53 timeline variants which are now styleguide-only) |
| D56 | 2026-04-26 | Apr 26 session | **Elias homepage trainer-card short bio: differentiate from Anthony, align with Bala bio.** Vasso (chat re: staging.cargonomics-site.pages.dev): "for elias short description it looks weird with similarity to tay" + "make it more aligned with the bio info supplied too". Both current short bios open "25+ years in [adj] shipping and logistics..." which reads as duplicated. Elias bio rewritten to lead with concrete Bala-PDF anchors (APL Vietnam MD, ZIM Country Manager, container shipping geographies) so the two openers diverge structurally. Same pattern likely needed on course.html `#course-instructors` (same Elias card markup). Role line + alt text NOT touched this turn (still says "Co-Founder & Lead Trainer" — conflicts with Bala's "Founder" framing but Vasso did not authorise role-line changes here; flagged as follow-up). | Medium. Trainer pair on the homepage is the highest-traffic surface for Elias's credibility; near-duplicate openers undermine both bios. | Edit `index.html` line 694 only: replace Elias `.trainer-card-v2__bio` paragraph with "Former Managing Director of APL Vietnam and Country Manager of ZIM Vietnam. 25 years in container shipping across India, the US, Sri Lanka, and Vietnam." (26 words, mirrors Anthony's 28-word two-sentence shape, opens with company titles instead of years for differentiation). Leave role + alt + Anthony untouched. | Small | Vasso | done (f3c924c, staging only; role-line + alt + course.html follow-ups still open) |
| D53 | 2026-04-26 | Apr 26 session | **Anthony Tay timeline parity** with Elias card on about.html. Vasso: "feels lopsided against Tay" + LinkedIn export of Anthony's career. After D52 landed, Elias's `.leader-card--timeline` carried 5 dated rows next to Anthony's plain paragraph bio, creating visual asymmetry in the 2-column leadership grid. Brings Anthony to parity using his LinkedIn data: Panalpina 1999-2005, APL Logistics Asia Director Sales/Marketing 2005-2011, APL Logistics Vietnam Country Director 2011-2018, APL Logistics Singapore global roles 2018-2023 (Senior Director / VP / Global Head, bundled), EvlvEdge Advisory Logistics Strategist since 2023. Bachelor of Supply Chain Management, Curtin University. Based in Singapore. Also corrects role line "Co-Founder / Coach" -> "Senior Trainer, Cargonomics" per the established no-cofounder rule (D41 follow-up that hadn't been applied to about.html). | High. Direct visual fix to D52's parity gap. Both leadership cards now structurally identical. | Edit `about.html` Anthony `.leader-card`: add `--timeline` modifier; replace paragraph bio with `__lede` + `__timeline` (5 rows) + `__meta` triple, mirroring Elias's structure. Anthony's APL Logistics Singapore phase (2018-2023) bundles three overlapping titles (Senior Director, VP, Global Head per LinkedIn) into one row — keeps the row count at 5 to match Elias. Update image alt to match new role line. | Small | Vasso | done (pending commit, staging only) |
| D52 | 2026-04-26 | Apr 26 session | **Elias bio overhaul on about.html, per Bala via Marilyn (24 Apr 18:21 email).** Vasso forwarded Bala's authoritative bio PDF + a layout target screenshot showing TRAINER-style timeline card. Roughly: replace the existing paragraph bio on the about.html leader-card with a richer structure (lede + 5-row career timeline + degree/location footer) using Bala's dates and phrasing. Authoritative bio: "Founder of Cargonomics, a Vietnam based training, recruitment, consulting and events management company"; 25 years container shipping (India, US, Sri Lanka, Vietnam); APL MD Vietnam 2011-2015; CMA CGM Group regional roles after; ZIM Country Manager Vietnam 2019-2025; board reps at VICT Port + Lotus Link (set up 2023); heads SingCham Vietnam Supply Chain Sector Committee since 2019; Master's NUS, based HCM City. | High. Replaces the v2 paragraph bio committed 2026-04-24 with the authoritative version Marilyn flagged was incoming. About-page leadership card is one of two main credibility surfaces for the bootcamp. | Edit `about.html` Elias `.leader-card`: change role from "Co-Founder / Coach" to "Founder, Cargonomics"; replace `<p class="leader-card__bio">` paragraph with `__lede` + `__timeline` + `__footer` triple. Add CSS in `fixes.css` for the new timeline subcomponents. CMA CGM dates 2015-2019 inferred from the gap between APL end (2015) and ZIM start (2019) per Bala's text — flagged in copy comment. Anthony's card kept as paragraph bio (no Bala update for him). About-section context (Leadership, not Trainers) so no "TRAINER" badge despite the screenshot. | Medium | Vasso | done (pending commit, staging only; index.html `#trainers` short-bio for Elias still uses the older "Former senior leadership at major international carriers across Southeast Asia" copy — flagged for follow-up if Vasso wants those propagated too) |
| D51 | 2026-04-23 | Apr 23 session | **Full-colour footer social icons site-wide.** Vasso: "also make these social icons in full colour". The existing `nth-of-type` CSS rules in `fixes.css` (v9, 2026-04-21) set brand-blue parent `color`, but the Zalo SVG is an outline-only `<circle fill="none" stroke="currentColor"/>` so it renders as a thin blue outline rather than a filled brand mark. FB and LinkedIn paths are single-colour blue monograms, already correct when `color` cascades — but vulnerable to CSS drift on WordPress/Elementor migration. | Medium. Visual polish. Brand fidelity on a shared chrome element that appears on every page. | Hardcode brand fills in the SVGs themselves (Zalo `#0068FF` + white Z; FB `#1877F2`; LinkedIn `#0A66C2`) rather than relying on `color` cascade. Zalo circle flips from outline to filled disc. White Z cutout for Zalo. Applied across all 5 live pages (index, about, course, contact, apply). CSS `nth-of-type` rules in `fixes.css` remain as belt-and-braces. | Small | Vasso | done (6bb9d60, staging only) |
| D41 | 2026-04-21 | Apr 21 session | **Second trainer named: Anthony Tay.** Vasso: "this is the other partner who will giving course with elias please update relevant mentions" + screenshot of the About page "Senior Industry Advisor" placeholder + LinkedIn profile PDF + profile photo. Marilyn's note on the source screenshot said "Coach number 2: LinkedIn profile link for photo and about Anthony Tay" — no title confirmed. | High. The placeholder "Senior Industry Advisor / Coming soon" treatment has been live on `index.html #trainers`, `course.html #course-instructors`, and `about.html #leadership` since 2026-04-16; all three now name Anthony Tay with bio drawn from his LinkedIn profile PDF. | 1) Copy `Anthony Tay Profile pic.jfif` → `img/anthony-tay.jpg`. 2) Archive originals (PDF + .jfif) in `06-client-comms/assets-from-marilyn/2026-04-21-anthony-tay-profile/`. 3) Replace `.trainer-card-v2--placeholder` on index.html + course.html with plain `.trainer-card-v2` + Anthony photo + short 2-sentence bio + `id="anthony"`. 4) Replace `.leader-card--placeholder` on about.html with full `.leader-card` + long LinkedIn-sourced bio + `id="about-anthony"`. 5) Replace "Senior Industry Advisor / To be announced" strings in styleguide.html v2 + v3 placeholder samples with generic "Trainer Name / Role Title" so the placeholder variant remains a valid reusable demo. 6) Update v2 trainer-card "Used on" row to note both slots filled. 7) Initial commit `f29f91e` over-claimed "Co-Founder" title for Anthony (inherited from the stale placeholder copy); Vasso corrected on review ("I did not say he is cofounder"). Follow-up commit strips Co-Founder everywhere: trainer cards show "Senior Trainer", About role line is "Senior Trainer, Cargonomics \| Founder, EvlvEdge Advisory", alt text updated. Marilyn to confirm final role/title via email (separate ask). | Medium | Vasso | in-progress (pushed to staging, awaiting Marilyn confirm on role + bio copy) |
| D57 | 2026-04-28 | Apr 28 session | **GA4 hostname filter — block tracking on staging / GitHub mirror / local / branch previews.** Vasso (chat re: staging.cargonomics-site.pages.dev): "in Google Analytics here I want to make it so that any traffic to the staging site is either going to be marked differently automatically or not, or not tracked at all... I would be guessing the site is cargonomics.com.vn". Then via screenshot of GA4 hostname filter: "these too (www.cargonomics.com.vn and cargonomics.com.vn is traffic wanted in the GA4 stats though)". GA4 Realtime was contaminated by `staging.cargonomics-site.pages.dev`, `cargonomics-site.pages.dev`, `vassovass.github.io`, `localhost` — every test click, every Marilyn preview review, every dev session was being counted alongside (eventually) real prod hits. | High. Without the filter, every Marilyn preview session and every Vasso dev hit lands in the same property as real applicants once the site goes live. Acquisition + behaviour reports become unreliable on day one. | Done in GTM (not GA4 — GA4 has no native hostname-exclusion admin filter, the "Internal Traffic" mechanism is IP-based). Created blocking trigger 32 `Block GA4 - Non-Production Hostname` (Custom Event type, regex `.*` event name + Page Hostname not equal to either prod hostname, using GTM API negation pattern `equals` + `negate=true` since v2 API has no `notEquals` type). Applied as exception on all 4 GA4 tags (Configuration 31, CTA Click 14, Attribution Loaded 26, Form Submit 29). Allowlist-prod approach so any future preview hostname is excluded automatically. Container version 3 published 2026-04-28. Documented in `docs/gtm-setup.md` "Hostname filtering" section. Historical pollution NOT cleaned up — Vasso explicitly skipped that ("dont worry re ga4 hostorical"); GA4 cannot delete events by hostname retroactively anyway, only via property-level filter at report time or full property reset. | Small | Vasso | done (GTM container v3 published; `docs/gtm-setup.md` doc commit pending) |
| D59 | 2026-04-28 | Apr 28 session | **Footer social icons clickable + FAB no-overlap on every page.** Vasso (chat): "the social icons on every single page is not clickable... and the WhatsApp icon overlaps when scrolling all the way down so we can't ever click on LinkedIn... robust best practice way." All 5 live pages rendered the 3 footer socials as `<span class="footer__social-placeholder">` with no `href`. Floating WhatsApp+Zalo stack (`position: fixed; bottom: 20px; right: 20px; z-index: 99`) overlays the footer LinkedIn icon at scroll-end, blocking the click even after the anchor is wired. | High. Three social CTAs dead on every page footer; FAB-vs-LinkedIn collision left LinkedIn unreachable even after a footer anchor is added. WordPress/Elementor migration constraint: portable solution, semantic anchors, tokens for colour, no DOM restructuring. | (1) Footer spans → anchors on all 5 HTML pages: `<a class="footer__social-link" href="..." target="_blank" rel="noopener noreferrer" data-cta-destination="zalo\|facebook\|linkedin" data-slot="footer-social-*">`. URLs: Zalo `https://zalo.me/84976875375` (reused from floating Zalo button), Facebook `https://www.facebook.com/CargonomicsJSC` (provided this session by Vasso), LinkedIn `https://www.linkedin.com/company/cargonomics/`. Brand-colour SVG fills migrated from hardcoded hex to `currentColor`; per-icon colour driven by `[data-cta-destination="..."]` attribute selectors in `fixes.css` consuming `--color-zalo` + new `--color-facebook` + `--color-linkedin` tokens (Cargonomics tokens already committed `1477609`; Flexed parity in this commit). (2) FAB overlap fix: new `js/floating-buttons.js` (33-line IIFE) uses IntersectionObserver — toggles `.floating-buttons--hidden` on `.floating-buttons` when `.footer` enters viewport at 35% threshold, fading FAB out (`opacity: 0; pointer-events: none; transform: translateY(16px)`); restores on scroll-up. Graceful degradation: if IO unsupported or JS off, FAB stays visible and the footer anchors are still clickable, so LinkedIn is reachable via footer. Elementor port: drops 1:1 into a Custom Code widget OR Elementor Pro's "Display Conditions → Element Visible" feature delivers the same effect with no code. Verification: paths/files correct on disk; local preview verification was hampered by HTTP cache + a stuck-style behaviour on the original anchor instance (a fresh DOM clone responded to inline `!important` correctly), so final visual confirmation deferred to staging deploy. Supersedes D8 (`.footer__social-link` CSS). D15 (WhatsApp/Zalo `href="#"` placeholders) narrows to the contact-info section only — floating buttons + footer socials now real. | Medium | Vasso | done (commit SHA pending — staging push) |
| D58 | 2026-04-28 | Apr 28 session | **GA4 internal-team opt-out — cookie-based block on prod hostnames.** Vasso (chat, after D57): "How do I block traffic to the main site from myself, Marilyn, and anyone from the team automatically... if they go to the dub, dub, dub or the www site?". Hostname filter from D57 doesn't catch internal hits to the live site since they're on the prod hostname. Three options offered (1: GA4 Internal Traffic IP filter, 2: cookie-based opt-out, 3: Google's browser opt-out extension). Vasso chose option 2. Same session: Vasso also asked for a Marilyn-facing email draft so she can forward the opt-out link to the internal team. After two iterations Vasso clarified the audience scope and added a bookmarkability requirement (see "Iterations" below). | High. Once the site goes live, every internal visit (Vasso testing, Marilyn reviewing, Lisa browsing) currently counts as a real visitor. Trial conversion funnel and content engagement reports get noisy on day one. Distribution scope matters: opting out partners or marketing leakage would defeat the point of the analytics. | GTM-side: created 1st-party Cookie variable 33 (`Cookie - cgn_internal`, reads `cgn_internal` cookie, no decode). Created blocking trigger 34 (`Block GA4 - Internal Opt-Out Cookie`, Custom Event regex `.*`, filter `Cookie - cgn_internal` equals `1`). Added trigger 34 as a second exception on all 4 GA4 tags alongside trigger 32 (so either hostname-not-prod OR cookie-set blocks GA4). Container version 4 published 2026-04-28. Site-side: inline opt-out script added to `<head>` of index/about/course/contact/apply BEFORE the GTM snippet. Reads `?internal=1` from URL → sets `cgn_internal=1` cookie for 1 year (with `domain=.cargonomics.com.vn` on prod hostnames so it covers both bare and www). `?internal=0` clears it. **Iterations same session:** (a) URL strip via `history.replaceState` was initially included; removed after Vasso requested bookmark support (Ctrl+D / Cmd+D), since the strip would have caused the bookmark to capture the URL without the `?internal=1` param. URL is now intentionally retained so a bookmark is a permanent re-trigger. Each click of the bookmark overwrites the cookie with a fresh 1-year `max-age`, effectively renewing the opt-out indefinitely. (b) Marilyn-facing email draft created at `06-client-comms/email-marilyn-internal-optout-draft.html`, then restructured to F-pattern format (Context / Action / Important section labels with bold-prefix + hyphen + dek, action items as bullets, "Important — never use externally" callout). (c) Audience scope clarified: forward to Cargonomics team / Elias / Anthony Tay / Anne Hill staff who review pages, but NEVER to partners, applicants, clients, marketing materials, or email signatures (partner referral traffic and external visits are tracked traffic we want to measure). Verified locally: cookie sets, URL retained, console logs, opt-back-in works. Distribution: send `https://www.cargonomics.com.vn/?internal=1` to internal reviewers only; each opens once per browser/device, recommended to bookmark. Optional pairing recommended in docs: GA4 Internal Traffic IP filter for the office WiFi (admin-only setup, no per-person friction). Cookie verification: open DevTools Console after visiting the URL → look for `[Cargonomics] Internal traffic opt-out ENABLED` info log. | Medium | Vasso | done (GTM container v4 published; site code in `index.html`/`about.html`/`course.html`/`contact.html`/`apply.html` head; email draft at `06-client-comms/email-marilyn-internal-optout-draft.html`; doc commit pending) |

---

## Done

| # | Date resolved | Description | Commit / PRD |
|---|---------------|-------------|--------------|
| D1 | 2026-04-16 | course.html testimonials section hidden (empty placeholder, visible as empty shell) | Commit `96744fb` (PRD 18 follow-through Phase 1) |
| D2 | 2026-04-16 | course.html FAQ section hidden (6 question headings with no answers) | Commit `96744fb` |
| D3 | 2026-04-16 | course.html instructors section hidden (placeholder text only) | Commit `96744fb` |
| D4 | 2026-04-16 | Homepage second trainer card removed ("Senior Industry Advisor" logo-opacity placeholder) | Commit `96744fb`. Archived in `docs/archived-components.md` id=`trainer-card-partner-placeholder` |
| D10 | 2026-04-16 | About page hero image: port-1.jpg replaced with hero-home.jpg via `<picture>` + 4:5/16:9 aspect constraint | Commit `96744fb` |
| D17 | 2026-04-16 | Trusted Partners section on homepage (11 partners + "+ 1,000 more" tile, text-first, logo-forward attributes) | Commit `dcb56db` |
| D18 | 2026-04-16 | Trusted Partners section on course page (adapted placement-first framing) | Commit `dcb56db` |
| D19 | 2026-04-16 | Apply page split: new `apply.html` holds application form, `contact.html` reduced to inquiries + contact-info, 9 Apply Now CTAs retargeted site-wide, sitemap + redirects + Course JSON-LD Offer URL all updated | Commit `ddb5d1f` |
| D21 | 2026-04-16 | About hero image stretched-landscape complaint: fixed via image swap + 2-column grid + aspect constraint | Commit `96744fb` |
| D22 | 2026-04-16 | Three Pillars on About text-heavy: image headers added to all 3 cards, number promoted, closing meta-sentence trimmed from each body | Commit `96744fb` |

---

## Changelog

| Date | Change |
|------|--------|
| 2026-04-14 | Created as Process A deliverable per approved April 14 plan. Seeded with 6 internal observations. |
| 2026-04-16 | Rebuilt from chat-ask backlog audit. Added D17-D28 from past-session mining + current session. Process fix: chat-ask logging rule codified in `.claude/rules/deploy-protocol.md` so future asks never drop. |
